<!DOCTYPE html>
<html lang="en">
<head>
    <%- include('../partials/head'); %>
</head>
<body class="container">

<header>
    <%- include('../partials/header'); %>
</header>

<main>
    <div class="menu">
    <h1>Sign up</h1>
    <p>To receive an occasional text message by a Fluxus artist enter your mobile phone number below. <br> You will not be charged and your number will be stored anonymously. You can cancel the service at any time. </p>
    <form method ='POST' action='/submit' onsubmit=SaveNumber()>
        <div class="form-group row">
            <label for="inputEmail3" class="col-sm-2 col-form-label">Telephone number (inc dialling code e.g '+44'):</label>
            <div class="col-sm-10">
            <input type="tel" autocomplete="tel" name="telephone"  class="form-control" id="inputtel" placeholder="+447816147120" minlength="10" maxlength="14" pattern="^\+(?:[0-9] ?){6,14}[0-9]$" required>
            </div>
        </div>
        <div class="form-group row">
            <div class="col-sm-10">
            <button type="submit" onclick="Error()"class="btn btn-primary">Sign up</button>
            </div>
        </div>
        </form>
    </div>
</main>

<footer>
    <%- include('../partials/footer'); %>
</footer>

</body>
<script>
function Error(){
$("h1").text("Uh Oh!..")
$(".menu p").text("Looks like the number wasn't successfully saved. Try Again, make sure to include all 11 digits")
 }
</script>
</html>